# The AI/BI dashboard for nyc_data_analysis_project's gold-layer tables.

resources:
  dashboards:
    yellow_tripdata_dashboard:
      display_name: "Yellow Taxi Trip & Price Hikes"
      file_path: ../src/dashboards/yellow_tripdata_dashboard.lvdash.json
      warehouse_id: ${var.warehouse_id}
      dataset_catalog: ${var.catalog}
      dataset_schema: ${var.schema}
